name: Starter get artifacts
on: [workflow_dispatch]
jobs:
  get-artifacts:
    runs-on: ubuntu-latest
    steps:
    - id: result 
      uses: machman5/list-artifacts@v1
    - run: echo '${{ steps.result.outputs.artifacts }}'
